{% extends 'base.html.twig' %}

{% block title %}MaSuperAgence - Gestion du bien {{ option.name }}{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-center">Gestion de l'option : {{ option.name }}</h1>
        <div class="col-md-6 mx-auto">
            {{ include('admin/option/_form.html.twig', {'button_label': 'Modifier'}) }}
            <a href="{{ path('option_index') }}">Retour à la liste</a>
        </div>
    </div>
{% endblock %}
