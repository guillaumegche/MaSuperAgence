<form method="post" action="{{ path('option_delete', {'id': option.id}) }}" style="display: inline-block" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette option ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
